<%- include("navbar.ejs") %>
<!-- <h2 class="text-primary mt-2 ms-3">Enquiry List</h2> -->

<div class="container mt-4">
  <div class="bg-primary text-white mt-3 ml-2 mr-2" style="height: 40px;"> 
                <h6 class="mb-0" style="font-size: 30px;margin-left: 10px;"></i>Enquiry List</h6>
            </div>
  <div class="row">
    <div class="col-12">
      <table class="table table-bordered table-striped">
        <thead>
          <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Email</th>
            <th>Mobile</th>
            <th>Subject</th>
            <th>Message</th>
            <th>Action</th>
          </tr>
        </thead>

        <tbody>
         
            
            <% for (let i = 0; i < enquiry.length; i++) { %>
              <tr>
                <td><%= i + 1 %></td>
                <td><%= enquiry[i].enquiry_name %></td>
                <td><%= enquiry[i].enquiry_email %></td>
                <td><%= enquiry[i].enquiry_number %></td>
                <td><%= enquiry[i].enquiry_subject %></td>
                <td><%= enquiry[i].enquiry_message %></td>
                <td>
                  <a href="/admin/enquiry/delete/<%= enquiry[i].enquiry_id %>" 
                     class="btn btn-danger btn-sm"
                     onclick="return confirm('Are you sure you want to delete this Enquiry?');">
                      <i class="bi bi-trash"></i>
                  </a>
                </td>
              </tr>
            <% } %>

        
           
        </tbody>
      </table>
    </div>
  </div>
</div>

<%- include("footer.ejs") %>
